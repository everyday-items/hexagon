# Hexagon 环境变量配置（完整模式）
#
# 复制此文件为 .env 并修改配置:
#   cp .env.example .env
#
# 此配置用于 docker-compose.yml（自包含模式，全部服务一键启动）

# ============== LLM API Keys ==============
# 请替换为您自己的 API Key
OPENAI_API_KEY=
DEEPSEEK_API_KEY=

# ============== 应用配置 ==============
APP_PORT=8000
LOG_LEVEL=info

# ============== DevUI 配置 ==============
DEVUI_PORT=8080

# ============== 基础设施连接地址 ==============
# 默认指向 Compose 内置服务，无需修改
# 如果要连接外部服务，取消注释并修改对应地址
#
# QDRANT_URL=http://your-qdrant-host:6333
# REDIS_URL=your-redis-host:6379
# POSTGRES_DSN=postgres://user:pass@your-pg-host:5432/hexagon?sslmode=disable

# ============== 基础设施端口映射 ==============
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334
REDIS_PORT=6379
REDIS_INSIGHT_PORT=8001
POSTGRES_PORT=5432

# ============== 基础设施认证 ==============
# 生产环境请务必修改默认密码
REDIS_PASSWORD=
POSTGRES_USER=hexagon
POSTGRES_PASSWORD=hexagon
POSTGRES_DB=hexagon
